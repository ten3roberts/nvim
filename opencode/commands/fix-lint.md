---
description: Run linter and fix all reported issues
---

Run the linter and fix all reported issues.

Lint output:
!`npm run lint 2>&1 || pnpm lint 2>&1 || yarn lint 2>&1 || true`

Fix each reported issue. Follow the existing code style and patterns in the codebase.

$ARGUMENTS
